# onprem-installer
Public tools and charts for onprem install

## Install

After the chart is published, users can install with:

```bash
curl -fsSL https://raw.githubusercontent.com/straiker-ai/onprem-installer/main/scripts/install.sh | bash
```

The installer adds the Helm repo from the public `gh-pages` branch.
