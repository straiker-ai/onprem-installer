{{- if .Values.nvidia.runtimeClass.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: {{ .Values.nvidia.runtimeClass.name }}
  labels:
    {{- include "base.labels" . | nindent 4 }}
handler: nvidia
{{- end }}
