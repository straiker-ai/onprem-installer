{{- /*
  No Secret is created by this chart for customer credentials.
  The pod mounts "straiker-secrets" via envFrom — create it before installing.
  See the straiker-platform README for the full key list and generation script.

  The session signing key (SYS__SESSION_SECRET_KEY) is auto-generated by the chart
  and stored in "straiker-session-secret". It is preserved across helm upgrades.
*/ -}}
