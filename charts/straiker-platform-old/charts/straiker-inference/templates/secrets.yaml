{{- /*
  No Secret is created by this chart.
  The pod references "straiker-secrets" via envFrom — create it before installing:
  kubectl create secret generic straiker-secrets \
    -n <namespace> \
    --from-literal=VLLM_API_KEY=<key>
*/ -}}
