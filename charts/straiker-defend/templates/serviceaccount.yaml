apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName }}
  namespace: {{ include "defend.namespace" . }}
  labels:
    {{- include "defend.labels" . | nindent 4 }}
  {{- if .Values.gcpServiceAccountEmail }}
  annotations:
    # Binds this KSA to the GCP SA via Workload Identity — GKE's equivalent
    # of EKS Pod Identity. Only meaningful when cloudProvider: gke.
    iam.gke.io/gcp-service-account: {{ .Values.gcpServiceAccountEmail }}
  {{- end }}
